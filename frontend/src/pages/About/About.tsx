import React from "react";
import styled from "@emotion/styled";

const Div = styled.div`
  min-height: 90vh;
`

const About = () => {
  return <Div>
    <p>About</p>
  </Div>
}

export default About
